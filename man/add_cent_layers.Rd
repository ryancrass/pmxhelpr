% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_plot.R
\name{add_cent_layers}
\alias{add_cent_layers}
\title{Internal helper: Add central tendency point, line, and error bar layers}
\usage{
add_cent_layers(
  plot,
  cent,
  y_var,
  point_el,
  line_el,
  eb_el,
  width,
  color_mapped = FALSE,
  show_errorbars = TRUE
)
}
\arguments{
\item{plot}{ggplot object to modify.}

\item{cent}{Character string specifying the central tendency measure.
One of \code{"mean"}, \code{"mean_sdl"}, \code{"mean_sdl_upper"}, \code{"median"}, \code{"median_iqr"}, or \code{"none"}.}

\item{y_var}{Character string specifying the y variable name (e.g., \code{"DV"}).}

\item{point_el}{A \code{pmx_point} element with point aesthetics.}

\item{line_el}{A \code{pmx_line} element with line aesthetics.}

\item{eb_el}{A \code{pmx_errorbar} element with error bar aesthetics.}

\item{width}{Numeric error bar cap width.}

\item{color_mapped}{Logical indicating whether color is mapped via
a global aesthetic (e.g., \code{col_var}). When \code{TRUE}, fixed theme colors
are suppressed to allow inheritance.}

\item{show_errorbars}{Logical indicating whether to add error bar layers.}
}
\value{
A modified ggplot object with central tendency layers added
}
\description{
Use this variant when color is either fixed (from theme) or inherited from
a global \code{col_var} aesthetic set in \code{\link[=init_plot]{init_plot()}}. The layers do not
contribute to a \code{scale_color_manual} legend.
}
\details{
For GOF-style overlay plots — multiple cent layers with different y-vars
(DV / PRED / IPRED) sharing a manual color legend — use
\code{\link[=add_cent_layers_manual]{add_cent_layers_manual()}} instead. The two helpers implement different
\code{aes()} contracts (inherited/fixed color vs. literal-label color routed
through \code{scale_color_manual()}) and intentionally are not unified.
}
\examples{
#

}
\keyword{internal}
