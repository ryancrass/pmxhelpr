% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_layers.R
\name{add_cent_layers_manual}
\alias{add_cent_layers_manual}
\title{Add central tendency layers for GOF overlay plots}
\usage{
add_cent_layers_manual(
  plot,
  cent,
  y_var,
  point_el,
  line_el,
  eb_el,
  width,
  color_aes,
  show_errorbars = TRUE
)
}
\arguments{
\item{plot}{ggplot object to modify.}

\item{cent}{Character string specifying the central tendency measure.
One of \code{"mean"}, \code{"mean_sdl"}, \code{"mean_sdl_upper"}, \code{"median"}, \code{"median_iqr"}, or \code{"none"}.}

\item{y_var}{Character string specifying the y variable name (e.g., \code{"DV"}).}

\item{point_el}{A \code{pmx_point} element with point aesthetics.}

\item{line_el}{A \code{pmx_line} element with line aesthetics.}

\item{eb_el}{A \code{pmx_errorbar} element with error bar aesthetics.}

\item{width}{Numeric error bar cap width.}

\item{color_aes}{String label for color aesthetic (e.g., \code{"DV"}, \code{"PRED"}).}

\item{show_errorbars}{Logical indicating whether to add error bar layers.}
}
\value{
A modified ggplot object with central tendency layers added
}
\description{
Use this variant for GOF-style overlay plots where multiple cent layers
with different y-vars (DV / PRED / IPRED) share a single manual color
legend. The layer's \code{color} is set to the literal string \code{color_aes}
inside \code{aes()} so ggplot treats it as a discrete level, then
\code{scale_color_manual()} (added by the calling plot function) maps each
level to a color. Fixed theme colors are never applied.
}
\details{
For single-y plots where color is inherited from a global \code{col_var}
aesthetic or fixed from the theme, use \code{\link[=add_cent_layers]{add_cent_layers()}}. The two
helpers implement different \code{aes()} contracts and intentionally are not
unified.
}
\keyword{internal}
