% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_layers.R
\name{add_trend_layers_style}
\alias{add_trend_layers_style}
\title{Internal helper: add a trend line layer (style pattern)}
\usage{
add_trend_layers_style(
  plot,
  method,
  show,
  se,
  series,
  col_var_str,
  col_trend,
  ...
)
}
\arguments{
\item{plot}{ggplot object.}

\item{method}{Smoothing method (\code{"loess"} or \code{"lm"}).}

\item{show}{Logical; whether to add the layer.}

\item{se}{Logical; whether to draw the SE ribbon.}

\item{series}{Series/role name for the style (e.g. \code{"loess"}, \code{"linear"}).}

\item{col_var_str}{Color variable name, or \code{NULL}.}

\item{col_trend}{Logical; stratify the trend by \code{col_var_str}.}

\item{...}{Passed to \code{\link[ggplot2:geom_smooth]{ggplot2::geom_smooth()}} (e.g. \code{formula}, \code{level}, \code{span}).}
}
\value{
Modified ggplot object.
}
\description{
Adds a \code{geom_smooth} for \code{method}, tagged as the \code{series} name (\code{"loess"} or
\code{"linear"}) so \code{\link[ggstylekit:style_plot]{ggstylekit::style_plot()}} fills its color, linewidth,
linetype, SE-ribbon fill (base \code{line_fill}), and SE-ribbon alpha (the
series \code{alphas} entry). When \code{col_trend} is \code{TRUE} and a color variable is
supplied, color and fill are mapped to it instead.
}
\keyword{internal}
