% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_internals.R
\name{build_style}
\alias{build_style}
\title{Internal helper: build a style_spec from preset defaults plus user overrides}
\usage{
build_style(defaults, overrides = list())
}
\arguments{
\item{defaults}{Named list of default \code{style_spec} arguments.}

\item{overrides}{Named list of user overrides (typically \code{list(...)}).}
}
\value{
A \code{ggstylekit_style_spec} object.
}
\description{
Each \verb{style_*()} preset supplies a named list of default \code{style_spec} fields.
Per-series map fields (\code{colors}, \code{fill}, \code{linetypes}, \code{alphas}, \code{shapes},
\code{sizes}, \code{linewidths}) are merged entry-wise onto the defaults so that a
partial override (e.g. \code{colors = c(obs_point = "red")}) keeps the other
roles' default values; all other fields replace their default wholesale. The
merged fields are passed to \code{ggstylekit::style_spec()}.
}
\keyword{internal}
