% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_check.R
\name{check_pipeline_args_dropped}
\alias{check_pipeline_args_dropped}
\title{Internal helper: abort if pipeline arguments are passed on the
precomputed-stats path.}
\usage{
check_pipeline_args_dropped(call, plot_only_args, fn_name)
}
\arguments{
\item{call}{A \code{call} object, typically \code{match.call()} evaluated in the
wrapper's frame.}

\item{plot_only_args}{Character vector of argument names that ARE honored
on the precomputed-stats path (e.g. \code{"theme"}, \code{"shown"}, \code{"pcvpc"}).
The matched data argument should also be included.}

\item{fn_name}{Character scalar, the wrapper's name, used in the error
message.}
}
\value{
\code{invisible(NULL)} on success.
}
\description{
Plot wrappers like \code{\link[=plot_vpc_cont]{plot_vpc_cont()}} and \code{\link[=plot_doseprop]{plot_doseprop()}} accept either
raw data or a precomputed \code{pmx_stats} container. On the cached path the
pipeline never re-runs, so any pipeline argument the user supplied would
be silently ignored. This helper inspects the caller's \code{match.call()},
subtracts the explicitly-allowed plot-only arguments, and aborts when the
remainder is non-empty — making the silent-shadow case audible.
}
\keyword{internal}
