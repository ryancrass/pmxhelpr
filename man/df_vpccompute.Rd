% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vpc_preprocess_df.R
\name{df_vpccompute}
\alias{df_vpccompute}
\title{Compute VPC quantile statistics from preprocessed data}
\usage{
df_vpccompute(
  data,
  pi = c(0.05, 0.95),
  ci = 0.9,
  bin_var = BIN_MID_VAR,
  strat_var = NULL,
  irep_name = "SIM",
  lower_bound = 0,
  mode = c("auto", "rank", "drop"),
  loq = attr(data, "loq")
)
}
\arguments{
\item{data}{Preprocessed simulation data, typically from \code{\link[=df_vpcpreprocess]{df_vpcpreprocess()}}.}

\item{pi}{Numeric vector of length 2 specifying prediction interval quantiles.
Default is \code{c(0.05, 0.95)}.}

\item{ci}{Numeric scalar in \verb{(0, 1)} specifying the simulation interval
(e.g. \code{0.90} for a 90\% CI). Default is \code{0.90}.}

\item{bin_var}{String. Binning variable name. Default is \code{"BIN_MID"}.}

\item{strat_var}{String or \code{NULL}. Stratification variable name. Default is \code{NULL}.}

\item{irep_name}{String. Replicate identifier column name. Default is \code{"SIM"}.}

\item{lower_bound}{Numeric. Forwarded to \code{\link[=var_predcorr]{var_predcorr()}} in the pc
flavor.}

\item{mode}{One of \code{"auto"} (default), \code{"rank"}, or \code{"drop"}. Controls how
BLQ-encoded values flow into quantile aggregation per-flavor: \code{"auto"}
uses \code{"rank"} for the std flavor and \code{"drop"} for the pc flavor (the
historical defaults); \code{"rank"} and \code{"drop"} apply to both flavors.}

\item{loq}{Numeric vector of unique LOQ values present in \code{data} (length 1
when LLOQ is constant), or \code{NULL}. Defaults to \code{attr(data, "loq")}. When
non-NULL, the std stats include \code{sim_prop_blq_low/med/hi} (BLQ detection
reads per-row thresholds from \code{data$LOQ}). The pc stats do \emph{not} emit a
\verb{pc_sim_prop_blq_*} set — LOQ has no meaning on the prediction-corrected
scale.}
}
\value{
A list of two data.frames with class \code{c("vpc_stats", "list")}:
\describe{
\item{\code{stats}}{Wide summary with both std and pc columns. Std columns
are unprefixed (\code{obs_low/med/hi}, \verb{sim_low_*} etc.); pc-flavor
observed and simulated quantile columns carry a \code{pc_} prefix
(\code{pc_obs_low}, \code{pc_sim_low_med}, etc.). \code{obs_n}, \code{obs_n_blq},
\code{obs_prop_blq}, \verb{sim_prop_blq_*}, \code{ci}, \code{pi_low}, \code{pi_hi} are
single (not duplicated). Carries attributes \code{n_replicates},
\code{loq}, and \code{strat_var}.}
\item{\code{obs}}{First-replicate observation rows with \code{MDV == 0},
carrying both \code{OBSDV} (std) and \code{PC_OBSDV} (prediction-corrected).}
}
}
\description{
Internal function. Always emits both standard and prediction-corrected
summary statistics: the std flavor uses raw \code{OBSDV}/\code{SIMDV}; the pc flavor
applies \code{var_loqcens} to \code{SIMDV} (when \code{loq} is supplied) and
\code{\link[=var_predcorr]{var_predcorr()}} per (\code{bin_var} × \code{strat_var} × \code{CMT}) to both
\code{OBSDV} and \code{SIMDV}. The pc columns are returned with a \code{pc_} prefix.
}
\keyword{internal}
