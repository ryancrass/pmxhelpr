% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/df_vpcstats.R
\name{df_vpcstats}
\alias{df_vpcstats}
\title{Compute VPC summary statistics from raw simulation data}
\usage{
df_vpcstats(
  data,
  time_var = TIME,
  ntime_var = NTIME,
  pred_var = PRED,
  sim_dv_var = SIMDV,
  obs_dv_var = OBSDV,
  strat_var = NULL,
  loq = NULL,
  irep_name = SIM,
  lower_bound = 0,
  mode = c("auto", "rank", "drop"),
  pi = c(0.05, 0.95),
  ci = 0.9
)
}
\arguments{
\item{data}{Raw simulation output. Must contain \code{EVID}, \code{MDV}, \code{PRED}, and
columns named by \code{time_var}, \code{ntime_var}, \code{sim_dv_var}, \code{obs_dv_var},
and \code{irep_name}.}

\item{time_var}{Column containing the actual time variable in \code{data}.
Accepts bare names or strings. Default is \code{TIME}.}

\item{ntime_var}{Column containing the nominal time variable in \code{data}.
Accepts bare names or strings. Default is \code{NTIME}.}

\item{pred_var}{Column containing population predictions in \code{data}.
Accepts bare names or strings. Default is \code{PRED}. Required for the
prediction-corrected statistics.}

\item{sim_dv_var}{Column containing simulated DV in \code{data}.
Accepts bare names or strings. Default is \code{SIMDV}.}

\item{obs_dv_var}{Column containing observed DV in \code{data}.
Accepts bare names or strings. Default is \code{OBSDV}.}

\item{strat_var}{Stratification variable. Accepts bare names or strings.
Default is \code{NULL}. Only a single stratifying variable is supported.}

\item{loq}{Numeric scalar, or \code{NULL}. When \code{NULL} and column \code{LLOQ} is
present in \code{data}, per-row \code{LLOQ} values are used as the censoring
threshold; the unique non-NA values are exposed via \code{config$loq} for
plotting. A scalar \code{loq} broadcasts to a constant threshold across rows.}

\item{irep_name}{Replicate identifier column. Accepts bare names or strings.
Default is \code{SIM}.}

\item{lower_bound}{Lower bound for prediction correction formula.}

\item{mode}{One of \code{"auto"} (default), \code{"rank"}, or \code{"drop"}. See
\code{\link[=plot_vpc_cont]{plot_vpc_cont()}} for semantics. \code{"auto"} resolves to \code{"rank"} for the
standard flavor and \code{"drop"} for the prediction-corrected flavor.}

\item{pi}{Numeric vector of length 2 specifying prediction interval quantiles.
Default is \code{c(0.05, 0.95)}.}

\item{ci}{Numeric scalar in \verb{(0, 1)} specifying the simulation interval
(e.g., \code{0.90} for a 90\% CI). Default is \code{0.90}.}
}
\value{
A list with two data.frames (class \code{c("vpc_stats", "list")}):
\describe{
\item{\code{stats}}{Wide summary statistics. Standard-VPC columns are
unprefixed (\code{obs_low/med/hi}, \code{sim_low_low/med/hi}, etc.);
prediction-corrected counterparts carry a \code{pc_} prefix
(\code{pc_obs_low}, \code{pc_sim_low_med}, etc.). \code{obs_n}, \code{obs_n_blq},
\code{obs_prop_blq}, \verb{sim_prop_blq_*}, \code{ci}, \code{pi_low}, \code{pi_hi} are
single (not duplicated). \verb{sim_prop_blq_*} is std-only — LOQ has
no meaning on the prediction-corrected scale. \code{-Inf} quantile
values from rank-mode fully-censored bins are masked to \code{NA}.
Carries attributes \code{n_replicates}, \code{loq}, and \code{strat_var}.}
\item{\code{obs}}{First-replicate observation rows with \code{MDV == 0}, used as
the scatter overlay in \code{\link[=plot_vpc_cont]{plot_vpc_cont()}}. \code{OBSDV} carries the
std-scale value; \code{PC_OBSDV} carries the prediction-corrected
counterpart.}
}
}
\description{
Returns the VPC summary statistics and observation overlay underlying a
continuous-data VPC plot. Takes raw simulation output (e.g., from
\code{\link[=df_mrgsim_replicate]{df_mrgsim_replicate()}}) and always emits \strong{both} standard and
prediction-corrected statistics in a single call. Preprocessing — column
renames, dose-row filtering, BLQ encoding, prediction-correction — is
applied internally.

For a plotted VPC, use \code{\link[=plot_vpc_cont]{plot_vpc_cont()}}, which is a thin wrapper that
calls \code{df_vpcstats()} and forwards the result to the internal plot
builder. The standard vs. prediction-corrected view is selected at plot
time via \code{plot_vpc_cont(pcvpc = TRUE/FALSE)}.
}
\examples{
model <- model_mread_load(model = "pkmodel")
simout <- df_mrgsim_replicate(data = data_sad, model = model, replicates = 10,
                              dv_var = ODV)
result <- df_vpcstats(simout, loq = 1)
head(result$stats)
head(result$obs)
}
\seealso{
Other vpc: 
\code{\link{is_pmx_vpc_plot}()},
\code{\link{is_vpc_stats}()},
\code{\link{plot_build_vpc}()},
\code{\link{plot_vpc_cont}()},
\code{\link{plot_vpc_legend}()},
\code{\link{plot_vpc_shown}()},
\code{\link{plot_vpc_theme}()}
}
\concept{vpc}
