% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_stats_class.R
\name{is_forest_stats}
\alias{is_forest_stats}
\title{Test whether an object is a \code{forest_stats} container}
\usage{
is_forest_stats(x, strict = FALSE)
}
\arguments{
\item{x}{Object to test.}

\item{strict}{Logical. When \code{TRUE}, additionally runs
\code{validate_forest_stats()} and returns \code{FALSE} on validation failure.
Default \code{FALSE} (class-tag check only, cheap).}
}
\value{
Logical scalar.
}
\description{
Predicate that returns \code{TRUE} if \code{x} carries the \code{"forest_stats"} class
-- i.e., it is the container returned by \code{\link[=df_forest]{df_forest()}} (also accepted by
\code{\link[=plot_forest]{plot_forest()}} and \code{\link[=plot_build_forest]{plot_build_forest()}} on the precomputed-stats
fast path).
}
\examples{
set.seed(1)
draws <- expand.grid(
  METRIC = c("AUC", "CMAX"),
  COV = c("WTBL", "AGE"),
  COVLVL = c("Low", "High"),
  REP = 1:50,
  stringsAsFactors = FALSE
)
draws$VALUE <- rlnorm(nrow(draws), 0, 0.2)
stats <- df_forest(
  draws,
  metric_var    = "METRIC",
  cov_name_var  = "COV",
  cov_level_var = "COVLVL",
  value_var     = "VALUE",
  replicate_var = "REP"
)
is_forest_stats(stats)                  # TRUE
is_forest_stats(as.data.frame(stats))   # FALSE -- coerced to plain frame
}
\seealso{
Other forest plot:
\code{\link[=df_forest]{df_forest()}},
\code{\link[=plot_build_forest]{plot_build_forest()}},
\code{\link[=plot_forest]{plot_forest()}},
\code{\link[=plot_forest_theme]{plot_forest_theme()}}
}
\concept{forest plot}
