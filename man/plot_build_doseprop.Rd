% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_doseprop.R
\name{plot_build_doseprop}
\alias{plot_build_doseprop}
\title{Build a dose-proportionality ggplot from a \code{doseprop_stats} object}
\usage{
plot_build_doseprop(stats, theme = NULL, se = TRUE)
}
\arguments{
\item{stats}{A \code{doseprop_stats} object (typically the output of
\code{\link[=df_doseprop]{df_doseprop()}}). Must carry the \code{obs}, \code{metric_var}, \code{exp_var},
\code{dose_var}, and \code{ci} attributes. Validated by \code{validate_doseprop_stats()}
at entry.}

\item{theme}{Named list of aesthetic parameters for the plot created by
\code{\link[=plot_doseprop_theme]{plot_doseprop_theme()}}. Defaults can be viewed by running
\code{plot_doseprop_theme()} with no arguments.}

\item{se}{logical to display confidence interval around regression. Default
is \code{TRUE}.}
}
\value{
a \code{ggplot} plot object
}
\description{
Constructs a log-log regression scatter plot from a \code{\link[=df_doseprop]{df_doseprop()}} result
(or any object satisfying the \code{doseprop_stats} contract). Recovers the
observation rows and column names from the object's attributes, builds the
faceting label from the per-metric \code{PowerCI} text, and renders the scatter
\itemize{
\item trend layers.
}

Most users will reach this function indirectly via \code{\link[=plot_doseprop]{plot_doseprop()}}.
Call \code{plot_build_doseprop()} directly when working from a manually-
constructed or cached \code{doseprop_stats} object — for example, plotting a
precomputed result from disk or a custom pipeline that produces compatible
columns and attributes.
}
\examples{
stats <- df_doseprop(dplyr::filter(data_sad_nca, PART == "Part 1-SAD"),
                      metrics = c("aucinf.obs", "cmax"))
plot_build_doseprop(stats)
plot_build_doseprop(stats, se = FALSE)
}
