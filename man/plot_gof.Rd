% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gof.R
\name{plot_gof}
\alias{plot_gof}
\title{Plot population overlay goodness-of-fit (GOF) plots}
\usage{
plot_gof(
  data,
  dv_var = DV,
  pred_var = PRED,
  ipred_var = IPRED,
  time_var = TIME,
  ntime_var = NTIME,
  shown = NULL,
  id_var = NULL,
  dose_var = DOSE,
  loq = NULL,
  loq_method = 0,
  blq_mode = c("obs", "all"),
  cent = c("mean", "mean_sdl", "mean_sdl_upper", "median", "median_iqr", "none"),
  dosenorm = FALSE,
  ref = NULL,
  log_y = FALSE,
  show_caption = TRUE,
  theme = NULL
)
}
\arguments{
\item{data}{Input dataset.}

\item{dv_var}{Column containing the dependent variable (DV).
Accepts bare names or strings. Default is \code{DV}.}

\item{pred_var}{Column containing population predictions (PRED).
Accepts bare names or strings. Default is \code{PRED}.}

\item{ipred_var}{Column containing individual predictions (IPRED).
Accepts bare names or strings. Default is \code{IPRED}.}

\item{time_var}{Column containing the actual time variable.
Accepts bare names or strings. Default is \code{TIME}.}

\item{ntime_var}{Column containing the nominal time variable.
Accepts bare names or strings. Default is \code{NTIME}.}

\item{shown}{Layer visibility settings created by \code{\link[=plot_gof_shown]{plot_gof_shown()}}.
Defaults can be viewed by running \code{plot_gof_shown()} with no arguments.}

\item{id_var}{Column to group observations for spaghetti lines.
Accepts bare names or strings. Default is \code{NULL} (no spaghetti lines).
Specifying a column (e.g., \code{id_var = ID}) enables spaghetti lines connecting
observations within each level of the variable.}

\item{dose_var}{Column to use in dosenormalization when \code{dosenorm} = TRUE.
Accepts bare names or strings. Default is \code{DOSE}.}

\item{loq}{Numeric value of the lower limit of quantification (LLOQ).
BLQ imputation behavior on the DV / prediction layers is controlled by
\code{blq_mode} and \code{loq_method}. Default is \code{NULL}.}

\item{loq_method}{Method for handling BLQ data. See \code{\link[=plot_dvtime]{plot_dvtime()}} for
option details. Default is \code{0} (no imputation).}

\item{blq_mode}{One of \code{"obs"} (default) or \code{"all"}. Controls which layers
receive BLQ imputation: \code{"obs"} imputes the observed \code{DV} layer only,
leaving \code{PRED} / \code{IPRED} untouched (mirrors \code{\link[=plot_dvtime]{plot_dvtime()}}); \code{"all"}
additionally imputes the prediction layers, useful when the GOF visual
should mirror an estimation engine that censored predictions to LLOQ.
Has no effect when \code{loq_method = 0}.}

\item{cent}{Character string specifying the central tendency measure to plot.

Options are:
\itemize{
\item Mean only: \code{"mean"} (default)
\item Mean +/- Standard Deviation (upper and lower error bar): \code{"mean_sdl"}
\item Mean + Standard Deviation (upper error bar only): \code{"mean_sdl_upper"}
\item Median only: \code{"median"}
\item Median +/- Interquartile Range: \code{median_iqr}
\item None: \code{"none"}
}}

\item{dosenorm}{logical indicating if observed data points should be dose normalized. Default is \code{FALSE},
Requires variable specified in \code{dose_var} to be present in \code{data}}

\item{ref}{Numeric y-intercept for a horizontal reference line, or \code{NULL} for
no reference line. For example, \code{ref = 0} draws a baseline reference for
change-from-baseline data.}

\item{log_y}{Logical indicator for log10 transformation of the y-axis. Also controls whether
the caption reports arithmetic or geometric mean when \code{show_caption = TRUE}.}

\item{show_caption}{Logical indicating if a caption should be shown describing the data plotted}

\item{theme}{Theme object created by \code{\link[=plot_gof_theme]{plot_gof_theme()}}.
Defaults can be viewed by running \code{plot_gof_theme()} with no arguments.
Default error bar width is 2.5\% of maximum \code{NTIME}.}
}
\value{
A \code{ggplot2} plot object
}
\description{
Creates a population overlay plot showing central tendency lines for
observed (DV), population predicted (PRED), and individual predicted (IPRED)
values. Colors and aesthetics for each variable are controlled through the
\code{theme} argument via \code{\link[=plot_gof_theme]{plot_gof_theme()}}. Use the \code{shown} argument to
selectively hide variables.
}
\examples{
plot_gof(data_sad_pkfit, dv_var = ODV, dosenorm = TRUE)

}
\seealso{
Other goodness-of-fit: 
\code{\link{plot_gof_shown}()},
\code{\link{plot_gof_theme}()}
}
\concept{goodness-of-fit}
