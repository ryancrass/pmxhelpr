% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vpc_cens.R
\name{plot_vpc_cens}
\alias{plot_vpc_cens}
\title{Plot a censoring (BLQ-proportion) VPC with exact time bins}
\usage{
plot_vpc_cens(
  data,
  time_var = TIME,
  ntime_var = NTIME,
  pred_var = PRED,
  sim_dv_var = SIMDV,
  obs_dv_var = OBSDV,
  strat_var = NULL,
  loq = NULL,
  irep_name = SIM,
  min_bin_count = 1,
  show_rep = TRUE,
  shown = NULL,
  theme = NULL,
  ci = 0.9
)
}
\arguments{
\item{data}{Input dataset. Simulated replicate data (typically from
\code{\link[=df_mrgsim_replicate]{df_mrgsim_replicate()}}) \strong{or} a precomputed \code{vpc_stats} container
(output of \code{\link[=df_vpcstats]{df_vpcstats()}}). When a precomputed container is passed,
the pipeline does not re-run and pipeline arguments are rejected; pass
raw data when you need pipeline control.}

\item{time_var}{Column containing the actual time variable in \code{data}.
Accepts bare names or strings. Default is \code{TIME}.}

\item{ntime_var}{Column containing the nominal time variable in \code{data}.
Accepts bare names or strings. Default is \code{NTIME}.}

\item{pred_var}{Column containing population predictions in \code{data}.
Accepts bare names or strings. Default is \code{PRED}. Required by
\code{\link[=df_vpcpreprocess]{df_vpcpreprocess()}} even though \code{PRED} does not appear in cens output.}

\item{sim_dv_var}{Column containing simulated DV in \code{data}.
Accepts bare names or strings. Default is \code{SIMDV}.}

\item{obs_dv_var}{Column containing observed DV in \code{data}.
Accepts bare names or strings. Default is \code{OBSDV}.}

\item{strat_var}{Stratification variable. Accepts bare names or strings.
Currently, only a single stratifying variable is supported.}

\item{loq}{Numeric scalar, or \code{NULL}. Lower limit of quantification (LLOQ).
Either \code{loq} or an \code{LLOQ} column in \code{data} is \strong{required} — without a
LOQ source, \code{df_vpcstats()} does not emit the \verb{sim_prop_blq_*} columns
a cens VPC needs. When \code{NULL} and column \code{LLOQ} is present in \code{data},
per-row \code{LLOQ} values are used as the censoring threshold; a scalar
\code{loq} broadcasts to a constant threshold across rows.}

\item{irep_name}{Name of replicate variable in \code{data}. Accepts bare names
or strings. Default is \code{SIM}.}

\item{min_bin_count}{Minimum number of observations per exact bin
required for inclusion in binned plot layers. Applied to total obs
(\code{obs_n} in the summary statistics frame); BLQ-encoded records \emph{do}
count toward this threshold, in contrast to \code{\link[=plot_vpc_cont]{plot_vpc_cont()}} which
requires quantifiable obs. Rationale: BLQ-heavy bins are the most
informative on a cens VPC and should be retained.}

\item{show_rep}{Display number of replicates as a plot caption. Default is \code{TRUE}.}

\item{shown}{Layer visibility settings created by \code{\link[=plot_vpc_shown]{plot_vpc_shown()}}. The
cens builder reads four of the keys: \code{obs_point}, \code{obs_median_line},
\code{sim_median_line}, \code{sim_median_ci}. Defaults follow \code{plot_vpc_shown()}:
the observed proportion line/points and simulated CI ribbon are shown;
the simulated median line is off by default. Pass
\code{plot_vpc_shown(sim_median_line = TRUE)} to enable it.}

\item{theme}{Named list of aesthetic parameters created by
\code{\link[=plot_vpc_theme]{plot_vpc_theme()}}. The cens builder reads the same four keys listed
above (\code{obs_point}, \code{obs_median_line}, \code{sim_median_line},
\code{sim_median_ci}); other keys are ignored.}

\item{ci}{Numeric scalar in \verb{(0, 1)} for the simulated CI bound on the BLQ
proportion across replicates (e.g., \code{0.90} for 90\% CI). Default is
\code{0.90}. Honored only on the raw-data path; passing \code{ci} explicitly
when \code{data} is a precomputed \code{vpc_stats} container raises an error.
The container's stored \code{ci} (set when \code{df_vpcstats()} was called) is
the source of truth on that path.}
}
\value{
A \code{pmx_vpc_plot} object (a \code{ggplot2} subclass). To access the
underlying VPC summary statistics directly, use \code{\link[=df_vpcstats]{df_vpcstats()}} and
inspect the \code{obs_prop_blq} and \verb{sim_prop_blq_*} columns of the
\code{stats} data.frame.
}
\description{
\code{plot_vpc_cens()} generates a VPC plot of the proportion of
below-limit-of-quantification (BLQ) observations over time and returns
a \code{ggplot2} object. Mirrors \code{\link[=plot_vpc_cont]{plot_vpc_cont()}} structurally but plots
the BLQ proportion rather than concentration quantiles: the simulated
non-parametric confidence band is the empirical distribution of
per-replicate BLQ proportions across \code{ci} bounds, and the observed
proportion is a single point/line per bin.

Thin wrapper that delegates to \code{\link[=df_vpcstats]{df_vpcstats()}} for computation and to
\code{\link[=plot_build_vpc]{plot_build_vpc()}} (\code{type = "cens"}) for plot construction. The
\verb{sim_prop_blq_*} columns produced by \code{\link[=df_vpcstats]{df_vpcstats()}} when a LOQ
source is available are the sole source of plot data.
}
\examples{
model <- model_mread_load(model = "pkmodel")
data_sad_pk <- dplyr::filter(data_sad, CMT \%in\% c(1, 2))
simout <- df_mrgsim_replicate(data = data_sad_pk, model = model,
                              replicates = 100, dv_var = ODV,
                              carry_out = c("LLOQ", "WTBL", "FOOD"),
                              recover = c("USUBJID", "PART"),
                              irep_name = SIM)

cens_plot <- plot_vpc_cens(data = simout, loq = 1, ci = 0.90)
}
\seealso{
Other vpc: 
\code{\link{df_vpcstats}()},
\code{\link{is_pmx_vpc_plot}()},
\code{\link{is_vpc_stats}()},
\code{\link{plot_build_vpc}()},
\code{\link{plot_vpc_cont}()},
\code{\link{plot_vpc_legend}()},
\code{\link{plot_vpc_shown}()},
\code{\link{plot_vpc_theme}()}
}
\concept{vpc}
