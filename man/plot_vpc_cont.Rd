% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vpc_cont.R
\name{plot_vpc_cont}
\alias{plot_vpc_cont}
\title{Plot a visual predictive check (VPC) for continuous data with exact time bins}
\usage{
plot_vpc_cont(
  sim,
  time_var = TIME,
  ntime_var = NTIME,
  pred_var = PRED,
  ipred_var = IPRED,
  sim_dv_var = SIMDV,
  obs_dv_var = OBSDV,
  strat_var = NULL,
  pcvpc = FALSE,
  loq = NULL,
  irep_name = SIM,
  min_bin_count = 1,
  show_rep = TRUE,
  lower_bound = 0,
  shown = NULL,
  theme = NULL,
  pi = c(0.05, 0.95),
  ci = c(0.05, 0.95),
  vpcstats = FALSE
)
}
\arguments{
\item{sim}{Input dataset. Must contain the following variables: \code{"ID"}, \code{"TIME"}}

\item{time_var}{Column containing the actual time variable in \code{sim}.
Accepts bare names or strings. Default is \code{TIME}.}

\item{ntime_var}{Column containing the nominal time variable in \code{sim}.
Accepts bare names or strings. Default is \code{NTIME}.}

\item{pred_var}{Column containing population predictions in \code{sim}.
Accepts bare names or strings. Default is \code{PRED}.}

\item{ipred_var}{Column containing individual predictions in \code{sim}.
Accepts bare names or strings. Default is \code{IPRED}.}

\item{sim_dv_var}{Column containing simulated DV in \code{sim}.
Accepts bare names or strings. Default is \code{SIMDV}.}

\item{obs_dv_var}{Column containing observed DV in \code{sim}.
Accepts bare names or strings. Default is \code{OBSDV}.}

\item{strat_var}{Stratification variable.
Accepts bare names or strings. Currently, only a single stratifying variable is supported.}

\item{pcvpc}{logical for prediction correction. Default is \code{FALSE}.}

\item{loq}{Numeric value of the lower limit of quantification (LLOQ) for the assay.
If \code{NULL} (default) and an \code{LLOQ} column is present in \code{sim}, the unique LLOQ value
is inherited from that column automatically.
Ignored when \code{pcvpc = TRUE} (LLOQ is not meaningful on the prediction-corrected scale).
Specifying this argument implies that the observed variable \code{OBSDV} is missing (\code{NA})
and \code{MDV = 1} where < \code{loq} in \code{sim}.
For standard VPCs (\code{pcvpc = FALSE}), all \code{MDV} values are set to 0 so that
all observations (including BLQ) are included in summary statistics.
Dashed horizontal line plotted at \code{loq} by default (controlled via \code{theme}).}

\item{irep_name}{Name of replicate variable in \code{sim}. Accepts bare names or strings. Default is \code{SIM}.}

\item{min_bin_count}{Minimum number of quantifiable observations in exact bin for inclusion
in binned plot layers. This argument drops small bins from summary statistic calculation
but retains these observations in the observed data points.}

\item{show_rep}{Display number of replicates as a plot caption. Default is \code{TRUE}.}

\item{lower_bound}{Lower bound for prediction correction formula.}

\item{shown}{Layer visibility settings created by \code{\link[=plot_vpc_shown]{plot_vpc_shown()}}.
Defaults can be viewed by running \code{plot_vpc_shown()} with no arguments.}

\item{theme}{Named list of aesthetic parameters for the plot created by \code{\link[=plot_vpc_theme]{plot_vpc_theme()}}.
Defaults can be viewed by running \code{plot_vpc_theme()} with no arguments.}

\item{pi}{Numeric vector of length 2 specifying prediction interval quantiles. Default is \code{c(0.05, 0.95)}.}

\item{ci}{Numeric vector of length 2 specifying confidence interval quantiles. Default is \code{c(0.05, 0.95)}.}

\item{vpcstats}{Logical. If \code{TRUE}, return a list of computed VPC statistics instead of a plot. Default is \code{FALSE}.}
}
\value{
A \code{ggplot2} object (default), or a \code{data.frame} of VPC summary
statistics from \code{\link[=df_vpcstats]{df_vpcstats()}} when \code{vpcstats = TRUE}.
}
\description{
\code{plot_vpc_cont()} generates a VPC plot using exact time bins
and returns a \code{ggplot2} object.
}
\examples{
model <- model_mread_load(model = "pkmodel")
data_sad_pk <- dplyr::filter(data_sad, CMT \%in\% c(1,2))
simout <- df_mrgsim_replicate(data = data_sad_pk, model = model, replicates = 100,
dv_var = ODV,
num_vars = c("CMT", "EVID", "MDV", "NTIME", "LLOQ", "WTBL", "FOOD"),
char_vars = c("USUBJID", "PART"),
irep_name = SIM)

vpc_plot <- plot_vpc_cont(
sim = simout,
pcvpc = TRUE,
pi = c(0.05, 0.95),
ci = c(0.05, 0.95))
}
