% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vpc_cont.R
\name{plot_vpc_cont}
\alias{plot_vpc_cont}
\title{Plot a visual predictive check (VPC) for continuous data with exact time bins}
\usage{
plot_vpc_cont(
  data,
  time_var = "TIME",
  ntime_var = "NTIME",
  pred_var = "PRED",
  sim_dv_var = "SIMDV",
  obs_dv_var = "OBSDV",
  strat_var = NULL,
  pcvpc = FALSE,
  loq = NULL,
  irep_name = "SIM",
  min_bin_count = 1,
  show_rep = TRUE,
  lower_bound = 0,
  mode = c("auto", "rank", "drop"),
  shown = NULL,
  theme = NULL,
  pi = c(0.05, 0.95),
  ci = 0.9
)
}
\arguments{
\item{data}{Input dataset. `}

\item{time_var}{Column containing the actual time variable in \code{data}.
Accepts bare names or strings. Default is \code{TIME}.}

\item{ntime_var}{Column containing the nominal time variable in \code{data}.
Accepts bare names or strings. Default is \code{NTIME}.}

\item{pred_var}{Column containing population predictions in \code{data}.
Accepts bare names or strings. Default is \code{PRED}.}

\item{sim_dv_var}{Column containing simulated DV in \code{data}.
Accepts bare names or strings. Default is \code{SIMDV}.}

\item{obs_dv_var}{Column containing observed DV in \code{data}.
Accepts bare names or strings. Default is \code{OBSDV}.}

\item{strat_var}{Stratification variable.
Accepts bare names or strings. Currently, only a single stratifying variable is supported.}

\item{pcvpc}{logical for prediction correction. Default is \code{FALSE}.}

\item{loq}{Numeric scalar, or \code{NULL}. Lower limit of quantification (LLOQ).
When \code{NULL} and column \code{LLOQ} is present in \code{data}, per-row \code{LLOQ}
values are used as the censoring threshold; a scalar \code{loq} broadcasts
to a constant threshold across rows. For standard VPCs (\code{pcvpc = FALSE})

\if{html}{\out{<div class="sourceCode">}}\preformatted{ + If a LOQ source is available (scalar `loq` or `LLOQ` column), all
 observations (including BLQ) are processed and censoring is performed
 at the quantile level. Filter to `EVID==0` so that doses are dropped.
 + If `loq=NULL` and `LLOQ` is NOT present in `data`, the dataset is
 filtered to `MDV==0` since `loq` is unknown.
}\if{html}{\out{</div>}}

For prediction-corrected VPCs (\code{pcvpc = TRUE})
+ If a LOQ source is available, all \code{SIMDV} and \code{OBSDV} values
\verb{< LOQ} (per row) are set to missing (\code{NA_real_}) so that both
observed and simulated data are censored in the same way before
quantile calculation.
+ If \code{loq=NULL} and \code{LLOQ} is NOT present in \code{data}, filter to
\code{MDV==0} since \code{loq} is unknown.
Dashed horizontal line plotted at each unique LLOQ value by default for
standard VPCs (controlled via \code{theme}); suppressed for \code{pcvpc = TRUE}
since \code{loq} has no meaning on the prediction-corrected scale.}

\item{irep_name}{Name of replicate variable in \code{data}. Accepts bare names or strings. Default is \code{SIM}.}

\item{min_bin_count}{Minimum number of quantifiable observations
(\code{obs_n - obs_n_blq} in the summary statistics frame) per exact bin
required for inclusion in binned plot layers. BLQ-encoded records
(\code{obs_n_blq}) do not count toward this threshold. This argument drops
small bins from summary statistic plotting but retains the underlying
observations as data points.}

\item{show_rep}{Display number of replicates as a plot caption. Default is \code{TRUE}.}

\item{lower_bound}{Lower bound for prediction correction formula.}

\item{mode}{One of \code{"auto"} (default), \code{"rank"}, or \code{"drop"}. Controls how
BLQ-encoded values are carried through quantile aggregation. In \code{"rank"}
mode, BLQ rows ranks low at \code{stats::quantile}; fully-censored quantiles
return \code{-Inf} and are masked to \code{NA} before plotting. In \code{"drop"} mode,
BLQ rows are excluded from quantile computation. \code{"auto"} resolves to
\code{"rank"} for std VPC and \code{"drop"} for pcVPC, matching the package's
historical behavior.}

\item{shown}{Layer visibility settings created by \code{\link[=plot_vpc_shown]{plot_vpc_shown()}}.
Defaults can be viewed by running \code{plot_vpc_shown()} with no arguments.}

\item{theme}{Named list of aesthetic parameters for the plot created by \code{\link[=plot_vpc_theme]{plot_vpc_theme()}}.
Defaults can be viewed by running \code{plot_vpc_theme()} with no arguments.}

\item{pi}{Numeric vector of length 2 specifying prediction interval quantiles. Default is \code{c(0.05, 0.95)}.}

\item{ci}{Numeric scalar in \verb{(0, 1)} for simulation interval (e.g., \code{0.90} for 90\% CI). Default is \code{0.90}.}
}
\value{
A \code{pmx_vpc_plot} object (a \code{ggplot2} subclass). To access the
underlying VPC summary statistics data.frame directly, use \code{\link[=df_vpcstats]{df_vpcstats()}}.
Adding \code{facet_wrap()} or \code{facet_grid()} to the result directly will warn,
as stratification must be specified via \code{strat_var} at call time.
}
\description{
\code{plot_vpc_cont()} generates a VPC plot using exact time bins
and returns a \code{ggplot2} object. Thin wrapper that delegates to
\code{\link[=df_vpcstats]{df_vpcstats()}} for computation and an internal builder for plot
construction.
}
\examples{
model <- model_mread_load(model = "pkmodel")
data_sad_pk <- dplyr::filter(data_sad, CMT \%in\% c(1,2))
simout <- df_mrgsim_replicate(data = data_sad_pk, model = model, replicates = 100,
dv_var = ODV,
carry_out = c("LLOQ", "WTBL", "FOOD"),
recover  = c("USUBJID", "PART"),
irep_name = SIM)

vpc_plot <- plot_vpc_cont(
data = simout,
pcvpc = TRUE,
loq = 1,
pi = c(0.05, 0.95),
ci = 0.90)
}
\seealso{
Other vpc: 
\code{\link{df_vpcstats}()},
\code{\link{is_pmx_vpc_plot}()},
\code{\link{is_vpc_stats}()},
\code{\link{plot_build_vpc}()},
\code{\link{plot_vpc_cens}()},
\code{\link{plot_vpc_legend}()},
\code{\link{plot_vpc_shown}()},
\code{\link{plot_vpc_theme}()}
}
\concept{vpc}
