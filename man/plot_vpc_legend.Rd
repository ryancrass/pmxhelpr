% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vpc_legend.R
\name{plot_vpc_legend}
\alias{plot_vpc_legend}
\title{Plot a legend for a visual predictive check (VPC)}
\usage{
plot_vpc_legend(
  ci = 0.9,
  pi = c(0.05, 0.95),
  shown = NULL,
  lloq = NULL,
  style = NULL,
  type = c("cont", "cens"),
  ...
)
}
\arguments{
\item{ci}{Numeric confidence level for simulation intervals (e.g., \code{0.90} for 90\% CI).
Should match argument passed to \code{\link[=plot_vpc_cont]{plot_vpc_cont()}}. Default is \code{0.90}.}

\item{pi}{prediction intervals plotted. Should match argument passed to \code{\link[=plot_vpc_cont]{plot_vpc_cont()}}. Default is c(0.05, 0.95).
Ignored when \code{type = "cens"} (cens VPCs do not have prediction intervals).}

\item{shown}{Layer visibility settings created by \code{\link[=plot_vpc_shown]{plot_vpc_shown()}}.
Defaults can be viewed by running \code{plot_vpc_shown()} with no arguments.}

\item{lloq}{Numeric scalar or vector of LLOQ values to label in the legend,
or \code{NULL} to omit. Each unique value becomes one legend entry rendered
with the theme's \code{loq_line} linetype. Pass \code{compute_out$config$loq} from
a \code{\link[=df_vpcstats]{df_vpcstats()}} result to mirror the reference lines drawn by
\code{\link[=plot_build_vpc]{plot_build_vpc()}}.}

\item{style}{A \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}} controlling legend aesthetics.
Defaults to \code{\link[=style_vpc]{style_vpc()}}; view the defaults by running \code{style_vpc()}
with no arguments. Should match the \code{style} passed to \code{\link[=plot_vpc_cont]{plot_vpc_cont()}}.}

\item{type}{One of \code{"cont"} (default) or \code{"cens"}. Selects the labels and
layer set the legend describes. Under \code{"cont"}, the central-tendency
entries are labeled \code{"Obs Med"}, \code{"Sim Med"}, and \code{"Sim <ci>\% CI Med"}
and the pi entries (\code{"Obs <p1>th and <p2>th"}, \code{"Sim <p1>th - <p2>th"},
\code{"Sim <ci>\% CI <p1>th and <p2>th"}) are included when their \code{shown}
keys are on. Under \code{"cens"}, the three central-tendency labels are
relabeled to \code{"Obs Prop BLQ"}, \code{"Sim Prop BLQ"}, \code{"Sim <ci>\% CI Prop BLQ"}
and all pi-related entries are suppressed regardless of \code{shown} (cens
VPCs have no prediction interval).}

\item{...}{Other arguments passed to \code{\link[ggplot2:theme]{ggplot2::theme()}}.}
}
\value{
a ggplot2 object
}
\description{
Plot a legend for a visual predictive check (VPC)
}
\examples{
plot_vpc_legend()
plot_vpc_legend(
pi = c(0.025, 0.975),
ci = 0.95,
 shown = plot_vpc_shown(obs_point = FALSE, obs_pi_line = TRUE,
 sim_pi_line = FALSE, sim_pi_area = FALSE, sim_pi_ci = TRUE,
 obs_median_line = TRUE,
 sim_median_line = FALSE, sim_median_ci = TRUE))
## Cens VPC legend
plot_vpc_legend(type = "cens", lloq = 1)
}
\seealso{
Other vpc:
\code{\link[=df_vpcstats]{df_vpcstats()}},
\code{\link[=is_pmx_vpc_plot]{is_pmx_vpc_plot()}},
\code{\link[=is_vpc_stats]{is_vpc_stats()}},
\code{\link[=plot_build_vpc]{plot_build_vpc()}},
\code{\link[=plot_vpc_cens]{plot_vpc_cens()}},
\code{\link[=plot_vpc_cont]{plot_vpc_cont()}},
\code{\link[=plot_vpc_shown]{plot_vpc_shown()}},
\code{\link[=style_vpc]{style_vpc()}}
}
\concept{vpc}
