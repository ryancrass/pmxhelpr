% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmx_element_class.R
\name{+.pmx_element}
\alias{+.pmx_element}
\title{Combine two pmx theme elements}
\usage{
\method{+}{pmx_element}(a, b)
}
\arguments{
\item{a}{A \code{pmx_element} object (left side, the "base").}

\item{b}{A \code{pmx_element} object of the same subclass as \code{a} (right side,
the "override"), or \code{NULL}. When \code{NULL}, \code{a} is returned unchanged.}
}
\value{
A \code{pmx_element} with the same class as \code{a}.
}
\description{
Overlay one \code{pmx_element} onto another of the same subclass. Each set
field in \code{b} overrides the matching field in \code{a}; fields unique to \code{a} are
unchanged. Useful for layering partial overrides on a base element:
\code{pmx_point(size = 2) + pmx_point(color = "red")} returns
\code{pmx_point(size = 2, color = "red")}.

Cross-subclass combinations (e.g. \code{pmx_style + pmx_point}) are
intentionally disallowed — \code{pmx_style} is a theme-level shortcut applied
at theme construction time, not a sibling element type.
}
\examples{
pmx_point(size = 2) + pmx_point(color = "red")
pmx_line(linewidth = 1) + NULL
}
