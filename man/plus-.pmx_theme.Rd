% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmx_theme_class.R
\name{+.pmx_theme}
\alias{+.pmx_theme}
\title{Combine two pmx plot themes}
\usage{
\method{+}{pmx_theme}(a, b)
}
\arguments{
\item{a}{A \code{pmx_theme} object (left side, the "base").}

\item{b}{A \code{pmx_theme} object (right side, the "override"), or \code{NULL}.
When \code{NULL}, \code{a} is returned unchanged.}
}
\value{
A \code{pmx_theme} with the same class vector as \code{a}.
}
\description{
Left-to-right merge of two \code{pmx_theme} objects. Each named entry in \code{b}
overrides the matching entry in \code{a}; entries unique to \code{a} are unchanged.
The class vector of \code{a} is preserved on the result, so a partial override
applied to a typed theme keeps its subclass (e.g.
\code{plot_vpc_theme() + pmx_theme(list(obs_point = ...))} is still a
\code{plot_vpc_theme}).
}
\examples{
base  <- plot_vpc_theme()
patch <- pmx_theme(list(obs_point = pmx_point(color = "red")))
out   <- base + patch
inherits(out, "plot_vpc_theme")   # TRUE -- class preserved
out$obs_point$color               # "red"

base + NULL                       # identical to `base`
}
