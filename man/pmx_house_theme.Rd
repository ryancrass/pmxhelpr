% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_internals.R
\name{pmx_house_theme}
\alias{pmx_house_theme}
\title{Internal helper: pmxhelpr house ggplot2 theme}
\usage{
pmx_house_theme(white_panel = FALSE, forest_panel = FALSE)
}
\arguments{
\item{white_panel}{Logical. When \code{TRUE}, sets \code{panel.background} to a white
rectangle with a thin black border (VPC family). Default \code{FALSE}.}

\item{forest_panel}{Logical. When \code{TRUE}, applies the forest-family panel
variant described above; \code{white_panel} is then ignored. Default \code{FALSE}.}
}
\value{
A ggplot2 theme object.
}
\description{
The shared base theme applied to every pmxhelpr plot via each preset's
\code{style_spec(theme = ...)} field: \code{theme_bw()} with the minor and vertical
major gridlines blanked. The VPC family passes \code{white_panel = TRUE} for a
white panel background with a thin black border.
}
\details{
The forest family (\code{forest_panel = TRUE}) inverts the gridline orientation:
it keeps \code{panel.grid.major.x} as a faint ruler aligned with the point
estimates and blanks \code{panel.grid.major.y} (the \code{facet_grid()} rows group the
y-axis by covariate name instead), uses a transparent panel with a thin grey
border, and styles the covariate row strips for \code{switch = "y"}: placed
outside the y-axis, no background fill, bold horizontal text right-aligned
to the panel.
}
\keyword{internal}
