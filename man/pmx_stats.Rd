% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmx_stats_class.R
\name{pmx_stats}
\alias{pmx_stats}
\title{Construct a \code{pmx_stats} container}
\usage{
pmx_stats(stats, obs = NULL, config = list(), subclass)
}
\arguments{
\item{stats}{A \code{data.frame} of per-row summary statistics.}

\item{obs}{A \code{data.frame} of observation rows for the scatter overlay, or
\code{NULL} when no overlay is used.}

\item{config}{A named \code{list} of run configuration (e.g. column names, CI
width, replicate count).}

\item{subclass}{Character scalar naming the concrete subclass (e.g.
\code{"vpc_stats"}, \code{"doseprop_stats"}).}
}
\value{
An object of class \code{c(subclass, "pmx_stats")}.
}
\description{
Constructor for the cacheable stats containers returned by
\code{\link[=df_vpcstats]{df_vpcstats()}} and \code{\link[=df_doseprop]{df_doseprop()}}. Each container is a named list with
three slots — \code{stats} (the per-row summary frame), \code{obs} (the observation
overlay used by plot builders, may be \code{NULL}), and \code{config} (named list of
run configuration consumed by the plot builders). The class vector is
\verb{c(<subclass>, "pmx_stats")}. The structural shape is enforced by
\code{\link[=validate_pmx_stats]{validate_pmx_stats()}} at construction time.
}
\seealso{
Other pmx stats class:
\code{\link[=is_pmx_stats]{is_pmx_stats()}}
}
\concept{pmx stats class}
