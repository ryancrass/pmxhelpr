% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmx_theme_class.R
\name{pmx_theme}
\alias{pmx_theme}
\title{Construct a \code{pmx_theme}}
\usage{
pmx_theme(elements = list(), subclass = NULL)
}
\arguments{
\item{elements}{Named list of \code{pmx_element} objects (e.g. as returned by
\code{\link[=pmx_point]{pmx_point()}}, \code{\link[=pmx_line]{pmx_line()}}, \code{\link[=pmx_ribbon]{pmx_ribbon()}}). May be empty. \code{NULL}
entries are dropped.}

\item{subclass}{Character scalar tag prepended to the class vector
(e.g. \code{"plot_vpc_theme"}). When \code{NULL} (default), the result carries
only \code{"pmx_theme"} — the right shape for a generic partial.}
}
\value{
An object of class \code{c(subclass, "pmx_theme")} (or just
\code{"pmx_theme"} when \code{subclass} is \code{NULL}).
}
\description{
Public factory for the shared \code{pmx_theme} base class. Builds a (possibly
partial) named list of \code{pmx_element} objects and tags it with a class
vector. Pair with \code{\link[=+.pmx_theme]{+.pmx_theme()}} to compose a partial override onto a
complete plot theme:

\if{html}{\out{<div class="sourceCode">}}\preformatted{base  <- plot_vpc_theme()
patch <- pmx_theme(list(obs_point = pmx_point(color = "red")))
base + patch
}\if{html}{\out{</div>}}

The five per-plot theme factories (\code{\link[=plot_dvtime_theme]{plot_dvtime_theme()}},
\code{\link[=plot_gof_theme]{plot_gof_theme()}}, \code{\link[=plot_dvconc_theme]{plot_dvconc_theme()}}, \code{\link[=plot_doseprop_theme]{plot_doseprop_theme()}},
\code{\link[=plot_vpc_theme]{plot_vpc_theme()}}) call \code{pmx_theme()} internally with their own subclass
tag, so user code rarely needs the \code{subclass} argument.
}
\examples{
# Generic partial — no subclass; useful as a `+.pmx_theme` right-hand side
pmx_theme(list(obs_point = pmx_point(color = "red")))

# Compose onto a complete theme
plot_vpc_theme() + pmx_theme(list(obs_point = pmx_point(color = "red")))
}
