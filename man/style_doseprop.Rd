% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_presets.R
\name{style_doseprop}
\alias{style_doseprop}
\title{Dose-proportionality plot style (\code{plot_doseprop})}
\usage{
style_doseprop(...)
}
\arguments{
\item{...}{Fields passed to \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}}, overriding the
defaults below. Per-series maps merge entry-wise onto the defaults; all
other fields replace their default wholesale.
A per-series map may instead be a palette \verb{function(n)} (e.g.
\code{function(n) grDevices::hcl.colors(n, "Viridis")}), which \code{ggstylekit} calls
with the number of mapped groups; a palette has no entries to merge, so it
replaces the default map.}
}
\value{
A \code{ggstylekit_style_spec} object.
}
\description{
Default \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}} for \code{\link[=plot_doseprop]{plot_doseprop()}}. Series names:
\code{obs_point}, \code{linear}. The builder draws the log-log axes and the
per-metric facet itself, so the \code{logx}, \code{logy}, and \code{facet} fields are
ignored; the facet layout fields \code{facet_scales} (default \code{"free"}),
\code{facet_nrow}, and \code{facet_ncol} are honored.
}
\examples{
style_doseprop()
}
\seealso{
Other dose proportionality:
\code{\link[=df_doseprop]{df_doseprop()}},
\code{\link[=is_doseprop_stats]{is_doseprop_stats()}},
\code{\link[=plot_build_doseprop]{plot_build_doseprop()}},
\code{\link[=plot_doseprop]{plot_doseprop()}}
}
\concept{dose proportionality}
