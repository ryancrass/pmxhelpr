% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_presets.R
\name{style_dvconc}
\alias{style_dvconc}
\title{Response versus concentration plot style (\code{plot_dvconc})}
\usage{
style_dvconc(...)
}
\arguments{
\item{...}{Fields passed to \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}}, overriding the
preset defaults; call the preset with no arguments to view them. Per-series maps merge entry-wise onto the defaults; all
other fields replace their default wholesale.
A per-series map may instead be a palette \verb{function(n)} (e.g.
\code{function(n) grDevices::hcl.colors(n, "Viridis")}), which \code{ggstylekit} calls
with the number of mapped groups; a palette has no entries to merge, so it
replaces the default map.}
}
\value{
A \code{ggstylekit_style_spec} object.
}
\description{
Default \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}} for \code{\link[=plot_dvconc]{plot_dvconc()}}. Series names:
\code{obs_point}, \code{ref_line}, \code{loess}, \code{linear}. The \code{loess}/\code{linear} trend lines
are \code{geom_smooth} (line entity); their SE ribbon fill/alpha come from
\code{line_fill}/\code{fill_alpha}.
}
\details{
Like every pmxhelpr preset, it places legend titles above the keys
(\code{legend.title.position = "top"}) and applies the house \code{theme_bw()}-derived
theme.
}
\examples{
style_dvconc()
style_dvconc(colors = c(loess = "blue", linear = "red"))
}
\seealso{
Other exploratory analysis:
\code{\link[=plot_dvconc]{plot_dvconc()}},
\code{\link[=plot_dvtime]{plot_dvtime()}},
\code{\link[=style_dvtime]{style_dvtime()}}
}
\concept{exploratory analysis}
