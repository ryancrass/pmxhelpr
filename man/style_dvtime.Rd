% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_presets.R
\name{style_dvtime}
\alias{style_dvtime}
\title{Concentration-time plot style (\code{plot_dvtime})}
\usage{
style_dvtime(...)
}
\arguments{
\item{...}{Fields passed to \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}}, overriding the
preset defaults; call the preset with no arguments to view them. Per-series maps (\code{colors}, \code{fill}, \code{shapes}, \code{sizes},
\code{linetypes}, \code{linewidths}, \code{alphas}) merge entry-wise onto the defaults, so
setting one role leaves the others unchanged (e.g. \code{shapes = c(obs_point = 16)}); all other fields replace their default wholesale (e.g. \code{title = "..."}).
A per-series map may instead be a palette \verb{function(n)} (e.g.
\code{function(n) grDevices::hcl.colors(n, "Viridis")}), which \code{ggstylekit} calls
with the number of mapped groups; a palette has no entries to merge, so it
replaces the default map.}
}
\value{
A \code{ggstylekit_style_spec} object.
}
\description{
Default \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}} for \code{\link[=plot_dvtime]{plot_dvtime()}}. Series names are the
plot roles: \code{obs_point}, \code{obs_line} (spaghetti), \code{cent_point}, \code{cent_line},
\code{cent_errorbar}, \code{ref_line}, \code{loq_line}.
}
\details{
The error bar cap width is the \code{errorbar_width} field (e.g.
\code{style_dvtime(errorbar_width = 10)}, in x-axis units). The preset leaves it
unset, and \code{\link[=plot_dvtime]{plot_dvtime()}} then defaults it to 2.5\% of the maximum nominal
time so the caps scale with the time axis.

Like every pmxhelpr preset, it places legend titles above the keys
(\code{legend.title.position = "top"}) and applies the house \code{theme_bw()}-derived
theme.
}
\examples{
style_dvtime()
style_dvtime(shapes = c(obs_point = 16, cent_point = 16))
style_dvtime(errorbar_width = 10)
}
\seealso{
Other exploratory analysis:
\code{\link[=plot_dvconc]{plot_dvconc()}},
\code{\link[=plot_dvtime]{plot_dvtime()}},
\code{\link[=style_dvconc]{style_dvconc()}}
}
\concept{exploratory analysis}
