% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_presets.R
\name{style_forest}
\alias{style_forest}
\title{Forest plot style (\code{plot_forest})}
\usage{
style_forest(...)
}
\arguments{
\item{...}{Fields passed to \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}}, overriding the
preset defaults; call the preset with no arguments to view them. Per-series maps merge entry-wise onto the defaults (e.g.
\code{shapes = c(point = 18)} leaves the other roles unchanged; \code{colors = c(FOOD = "red")} adds a covariate color while keeping the role colors);
all other fields replace their default wholesale.
A per-series map may instead be a palette \verb{function(n)} (e.g.
\code{function(n) grDevices::hcl.colors(n, "Viridis")}), which \code{ggstylekit} calls
with the number of mapped groups; a palette has no entries to merge, so it
replaces the default map.}
}
\value{
A \code{ggstylekit_style_spec} object.
}
\description{
Default \code{\link[ggstylekit:style_spec]{ggstylekit::style_spec()}} for \code{\link[=plot_forest]{plot_forest()}} /
\code{\link[=plot_build_forest]{plot_build_forest()}}. Series names are the plot roles: \code{point} (the
estimate), \code{errorbar} (the CI line range), \code{ref_line} (the vertical
reference line drawn at \code{ref}), and \code{ref_band} (the shaded equivalence band
drawn at \code{ref_band}; a \code{geom_rect} annotation whose \code{fill} and \code{alpha} come
from the \code{fill} / \code{alphas} maps).
}
\details{
Per-covariate coloring is opt-in through the same \code{colors} map: add entries
keyed by covariate name (the values of the \code{cov_name_var} column, e.g.
\code{colors = c(FOOD = "darkgreen", WTBL = "darkblue")}). When at least one
covariate present in the plotted data has an entry, the builder maps point
and error bar color to the covariate (facet) via \code{scale_color_manual()} and
suppresses the legend, since the facet strips already label each covariate.
Covariates without an entry render in \code{grey50} with a warning. The role
entries (\code{point}, \code{errorbar}, \code{ref_line}) are then ignored for the mapped
layers.

The builder draws the covariate row facet itself (\code{facet_grid(rows = cov_name, switch = "y")}), so the \verb{facet*} fields are ignored, and it sets
the plot title to the rendered metric unless the \code{title} field is supplied.
The house theme uses the forest panel variant of \code{theme_bw()}: a faint
vertical ruler, no horizontal gridlines, and bold covariate strips placed
outside the y-axis.
}
\examples{
style_forest()
style_forest(shapes = c(point = 18), sizes = c(point = 3))
style_forest(fill = c(ref_band = "skyblue"), alphas = c(ref_band = 0.2))
style_forest(colors = c(FOOD = "firebrick", WTBL = "steelblue", Reference = "grey20"))
}
\seealso{
Other forest plot:
\code{\link[=df_forest]{df_forest()}},
\code{\link[=is_forest_stats]{is_forest_stats()}},
\code{\link[=plot_build_forest]{plot_build_forest()}},
\code{\link[=plot_forest]{plot_forest()}}
}
\concept{forest plot}
