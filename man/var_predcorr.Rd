% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{var_predcorr}
\alias{var_predcorr}
\title{Apply prediction correction}
\usage{
var_predcorr(dv_var, pred_var, lower_bound = 0)
}
\arguments{
\item{dv_var}{Vector containing the dependent variable (DV)}

\item{pred_var}{Vector containing population predictions (PRED)}

\item{lower_bound}{Lower bound for prediction correction formula.}
}
\value{
A numeric vector of prediction-corrected values of \code{dv_var}
}
\description{
Apply prediction correction
}
\details{
The bin-median \code{predbin} is computed over the full \code{pred_var}
vector passed in. Callers must group their data by a binning variable
(e.g. nominal time, optionally stratified by compartment or covariates)
before invoking \code{var_predcorr()} so that \code{predbin} is the median across
the bin's observations, not the entire dataset.
}
\examples{
pkmodel <- model_mread_load(model = "pkmodel")
data <- df_mrgsim_addpred(
  data = dplyr::filter(data_sad, CMT != 3),
  model = pkmodel)
data <- data |>
  dplyr::filter(EVID == 0) |>
  dplyr::group_by(NTIME) |>
  dplyr::mutate(PCDV = var_predcorr(ODV, PRED)) |>
  dplyr::ungroup()

}
\seealso{
Other vectorized helpers:
\code{\link[=var_addn]{var_addn()}},
\code{\link[=var_dosenorm]{var_dosenorm()}}
}
\concept{vectorized helpers}
