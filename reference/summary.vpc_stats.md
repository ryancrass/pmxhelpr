# Summary method for `vpc_stats`

Same content as
[`print.vpc_stats()`](https://ryancrass.github.io/pmxhelpr/reference/print.vpc_stats.md)
but without the head preview. Suitable for vignette output and test
snapshots.

## Usage

``` r
# S3 method for class 'vpc_stats'
summary(object, ...)
```

## Arguments

- object:

  A `vpc_stats` object.

- ...:

  Currently unused.

## Value

`invisible(object)`.
